{
  "name": "dynamic-timetable-resolver-server",
  "version": "1.0.0",
  "description": "Backend API for Dynamic Timetable Conflict Resolver",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "seed": "node -e \"require('dotenv').config(); require('./src/config/db')().then(require('./src/seed')).then(() => process.exit(0))\"",
    "test": "node tests/conflictEngine.test.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-validator": "^7.2.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.3.0",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
